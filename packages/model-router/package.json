{
  "name": "@metapulse/model-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Provider-agnostic AI routing. Runs with Claude, Gemini, both, or neither configured — resolves per task, retries the next configured provider on failure, and only throws ProviderUnavailableError when nothing is configured at all. Consumers (the agent graphs) never import @anthropic-ai/sdk or @google/genai directly.",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@metapulse/db": "workspace:*",
    "@anthropic-ai/sdk": "^0.112.4",
    "@google/genai": "^2.12.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
