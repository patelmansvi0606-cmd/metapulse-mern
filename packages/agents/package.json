{
  "name": "@metapulse/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Content Graph and Engagement Graph, their deterministic fallbacks, and RunRecorder — the audit trail deliberately decoupled from LangGraph's own checkpoint format.",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@metapulse/db": "workspace:*",
    "@metapulse/schemas": "workspace:*",
    "@metapulse/model-router": "workspace:*",
    "@langchain/langgraph": "^1.4.8",
    "@langchain/core": "^1.2.3",
    "mongoose": "^9.8.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
